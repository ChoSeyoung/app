/**
 * 생성된 음식 이미지 매핑 파일.
 *
 * 역할:
 * - 로컬에서 생성된 이미지 파일을 앱 자산으로 연결한다.
 * - image:sync 스크립트가 자동으로 다시 쓴다.
 */

export const GENERATED_RECIPE_IMAGES: Record<string, number> = {
  "ricePumpkin": require("../assets/generated/recipes/ricePumpkin.png"),
};

export const GENERATED_INGREDIENT_IMAGES: Record<string, number> = {
  "seed-rice": require("../assets/generated/ingredients/seed-rice.png"),
};
